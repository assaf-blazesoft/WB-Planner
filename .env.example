# Supabase — copy from: supabase.com → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Netlify function base URL (local dev only — leave blank for production)
VITE_FUNCTIONS_URL=

# Set these in Netlify dashboard → Environment Variables (never commit real values)
RESEND_API_KEY=
FROM_EMAIL=
APP_URL=
