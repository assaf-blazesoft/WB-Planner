# Supabase — copy from: supabase.com → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Email function env vars — set in Cloudflare Pages dashboard (never commit real values)
RESEND_API_KEY=
FROM_EMAIL=
APP_URL=
